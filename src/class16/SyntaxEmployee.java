package class16;

public class SyntaxEmployee {
    // if variable can be different then, create a instance method
    // if variable is same as CEO IS SUMAIR, then create a static variable
    String empID;// we created a instance method because ID is different for each employee
    double salary;// we created a instance method because ID is different for each employee
    String CEO="Sumair";//is going to be same we create this one as static
}
